[project]
name = "engineering-method"
version = "0.2.0"
description = "Risk-proportionate engineering workflows for Codex, Claude and OpenCode."
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.unittest]
start-directory = "tests"

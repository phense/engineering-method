# Backlog

This template is rendered by `engineering_method.backlog` with validated machine markers immediately before every task. The local document is canonical until a writable GitHub repository has been migrated; then the generated cache is read-only.
